import discord from 'discord.js';

// create date data type where date is defined by DD-MM-YYYY (all years are 0000)

export interface BirthdayDefinition {
    username: string;
    userid: number;
    // birthdate: date;
    // lastnotified: date;
}
